services:
  - type: web
    name: h-app-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true